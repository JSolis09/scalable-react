// import { fromJS } from 'immutable';
import { selectNavigationContainerDomain } from '../selectors';

describe('selectNavigationContainerDomain', () => {
  it('should be defined', () => {
    expect(selectNavigationContainerDomain).toBeDefined();
  });
});
