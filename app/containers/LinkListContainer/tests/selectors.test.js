// import { fromJS } from 'immutable';
import { selectLinkListContainerDomain } from '../selectors';

describe('selectLinkListContainerDomain', () => {
  it('should be defined', () => {
    expect(selectLinkListContainerDomain).toBeDefined();
  });
});
